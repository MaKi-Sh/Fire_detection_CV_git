# ---- STEP 1. Clone CVAT repo ----
git clone https://github.com/cvat-ai/cvat
cd cvat

# ---- STEP 2. Start CVAT with Docker ----
docker compose up -d

# Check that containers are running
docker ps | grep cvat

# Test in browser: http://localhost:8080

# ---- STEP 3. Start ngrok tunnel (global access) ----
# This will forward https://xxxx.ngrok-free.app to your local CVAT
ngrok http --host-header=rewrite 8080

# Copy the HTTPS link ngrok shows (e.g. https://abcd-1234.ngrok-free.app)
# Share that link to let anyone in the world access your CVAT

# ---- STEP 4. Stop services ----
# Stop ngrok by pressing Ctrl+C in the ngrok terminal
# Then stop CVAT containers:
docker compose down

